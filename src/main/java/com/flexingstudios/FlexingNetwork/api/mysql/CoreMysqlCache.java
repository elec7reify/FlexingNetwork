package com.flexingstudios.FlexingNetwork.api.mysql;

import java.util.List;

public class CoreMysqlCache {
    public static void save(String table, Object key, List<String> columns, List<Object> values) {
        //save(table, key, columns.<String>toArray(new String[columns.size()]), values.toArray());
    }
}
